import { defineStore } from 'pinia'
import type { ESiteState } from '@/domain/ESiteState';

export const useCommerceStore = defineStore('whereismynextoffsite', {
  state: (): ESiteState => ({
   
  }),
  getters: {
  },
  actions: {
    
  }
})

